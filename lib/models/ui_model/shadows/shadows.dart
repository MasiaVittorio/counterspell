import 'package:flutter/material.dart';

class CSShadows{
  static const Color color = Color(0x59000000);
  static const double blur = 12.0;

  static const BoxShadow shadow = BoxShadow(
    color: color, 
    blurRadius: blur,
  );

}