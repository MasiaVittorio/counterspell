import 'model.dart';
import 'package:counter_spell_new/core.dart';

class CommanderStatsScreen extends StatelessWidget {

  final CommanderStats stats;

  const CommanderStatsScreen(this.stats);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}