import 'package:counter_spell_new/core.dart';

class TutorialCommander extends StatelessWidget {
  const TutorialCommander();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Icon(CSIcons.damageIconOutlined, size: 120),
    );
  }
}