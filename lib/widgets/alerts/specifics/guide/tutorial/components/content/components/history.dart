import 'package:counter_spell_new/core.dart';

class TutorialHistory extends StatelessWidget {
  const TutorialHistory();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Icon(McIcons.timer_sand, size: 120),
    );
  }
}