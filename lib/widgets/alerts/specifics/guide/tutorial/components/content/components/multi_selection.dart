import 'package:counter_spell_new/core.dart';

class TutorialSelection extends StatelessWidget {
  const TutorialSelection();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Icon(Icons.check_box, size: 120),
    );
  }
}