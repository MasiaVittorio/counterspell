import 'package:counter_spell_new/core.dart';

class TutorialScroll extends StatelessWidget {
  const TutorialScroll();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Icon(McIcons.gesture_swipe_horizontal, size: 120),
    );
  }
}