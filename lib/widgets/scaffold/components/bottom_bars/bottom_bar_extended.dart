import 'package:flutter/material.dart';

class CSBottomBarExtended extends StatelessWidget {
  const CSBottomBarExtended({Key key}): super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}